$('.daily_posts_table').html(`
  <h2>検索結果</h2>
  <div class="card" >
    <div class="card-header">投稿数</div>
    <div class="card-body">
      <%= j( @books.count ) %>
    </div>
  </div>
`)

<!--$("#search_result").html("<%#= j(render 'users/count-search-result', search_book: @search_book) %>");-->