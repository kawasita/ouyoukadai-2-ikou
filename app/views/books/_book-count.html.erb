<h1>投稿数の前日比・前週比</h1>
<div class = "table_width">
   <table class="table table-bordered">
    <thead>
     <tr>
      <th>今日の投稿数</th>
      <th>前日の投稿数</th>
      <th>前日比</th>
     </tr>
    </thead>
    <tbody>
     <tr>
     　<td><%= today_book.count %></td>
     　<td><%= yesterday_book.count %></td>
     　<td>
       <% if yesterday_book.count == 0 %>
        前日の投稿はありません
       <% else %>
       　<% today_book.count / yesterday_book.count  %>
       <% end %>
      </td>
     </tr>
    </tbody>
   </table>
  </div>